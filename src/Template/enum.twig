{% if docComment %}
/**
 * {{ docComment|cleanDocComment|raw }}
 */
{% endif %}
export enum {{ className }} {
{% for case in cases %}
  {{ case.name }} = {% if isStringEnum %}'{{ case.value }}'{% else %}{{ case.value }}{% endif %},
{% endfor %}
}
